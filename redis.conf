# Network
bind 0.0.0.0
protected-mode no
port 6379

# Disable RDB automatic save (nếu không cần)
save ""
# Tắt cơ chế ghi log từng lệnh (AOF - Append Only File).
appendonly no

# Memory
maxmemory 512mb
maxmemory-policy allkeys-lru

# Giữ kết nối TCP sống mỗi 60 giây
tcp-keepalive 60
# Vô hiệu hóa auto-timeout client → không kill kết nối khi client treo/chậm
timeout 0
# Tần suất Redis chạy các tác vụ background như TTL expire check, cleanup, etc.
hz 10ps


# Rename nhạy cảm
rename-command SLAVEOF ""
rename-command FLUSHALL ""
rename-command FLUSHDB ""
rename-command CONFIG ""
rename-command SHUTDOWN ""


### Production
# # ---------------------
# # NETWORK SECURITY
# # ---------------------
#
# # Chỉ listen nội bộ hoặc IP của VPC/private subnet
# bind 127.0.0.1
# # hoặc: bind 10.0.0.5 (nếu Redis chạy trong private subnet của cloud)
#
# protected-mode yes
# # Luôn bật chế độ bảo vệ nếu không dùng password hoặc ACL
#
# port 6379
#
# requirepass yourStrongPasswordHere
# # 🔐 Bắt buộc client phải xác thực password trước khi truy cập Redis
#
# # Nếu dùng Redis 6+ và muốn dùng ACL thay vì password, có thể dùng:
# # user default on nopass ~* +@all
# # (hoặc cấu hình ACL chi tiết hơn qua file aclfile)
#
# # ---------------------
# # PERSISTENCE (OFF nếu chỉ dùng như cache)
# # ---------------------
#
# save ""
# appendonly no
#
# # ---------------------
# # MEMORY & TTL CACHE
# # ---------------------
#
# maxmemory 512mb
# maxmemory-policy allkeys-lru
#
# # ---------------------
# # CONNECTION TUNING
# # ---------------------
#
# tcp-keepalive 60
# timeout 0
# hz 10
#
# # ---------------------
# # DISABLE DANGEROUS COMMANDS
# # ---------------------
#
# rename-command FLUSHALL ""
# rename-command FLUSHDB ""
# rename-command CONFIG ""
# rename-command SHUTDOWN ""
# rename-command DEBUG ""
# rename-command SLAVEOF ""
#
# # ---------------------
# # LOGGING
# # ---------------------
#
# loglevel notice

