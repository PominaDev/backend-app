<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pomina.commonservices.product_warranty_activation.mapper.ProductMapper">

    <select id="findById">
        SELECT master_product_id,
               ma_cuon_ton,
               ten_san_pham,
               loai_cuon,
               bh_phai_mau,
               bh_an_mon,
               total_weight,
               total_length,
               m_a_length,
               m_b_length,
               m_c_length,
               master_prod_type_id,
               status,
               noted,
               created_at,
               created_by,
               updated_at,
               updated_by,
               is_deleted
        FROM master_product
        WHERE master_product_id = #{id}
          AND is_deleted = 0
    </select>


    <select id="findAll">
        SELECT master_product_id,
               ma_cuon_ton,
               ten_san_pham,
               loai_cuon,
               bh_phai_mau,
               bh_an_mon,
               total_weight,
               total_length,
               m_a_length,
               m_b_length,
               m_c_length,
               master_prod_type_id,
               status,
               noted,
               created_at,
               created_by,
               updated_at,
               updated_by,
               is_deleted
        FROM master_product
        WHERE is_deleted = 0
    </select>

    <select id="findAllPaged">
        SELECT master_product_id,
        ma_cuon_ton,
        ten_san_pham,
        loai_cuon,
        bh_phai_mau,
        bh_an_mon,
        total_weight,
        total_length,
        m_a_length,
        m_b_length,
        m_c_length,
        master_prod_type_id,
        status,
        noted,
        created_at,
        created_by,
        updated_at,
        updated_by,
        is_deleted
        FROM master_product
        WHERE is_deleted = 0
        <if test="paging != null and paging.orderByClause != null">
            ORDER BY ${paging.orderByClause}
        </if>
        LIMIT #{limit} OFFSET #{offset}
    </select>


    <insert id="insert">
    </insert>


    <update id="update">
    </update>


    <delete id="deleteById">
        DELETE
        FROM master_product
        WHERE master_product_id = #{id}
    </delete>


    <update id="softDeleteById">
        UPDATE master_product
        SET is_deleted = 1
        WHERE master_product_id = #{id}
    </update>


    <select id="existsById" resultType="boolean">
        SELECT EXISTS(
            SELECT 1
            FROM master_product
            WHERE master_product_id = #{id} AND is_deleted = 0
        )
    </select>


    <select id="countAll" resultType="int">
        SELECT COUNT(master_product_id)
        FROM master_product
        WHERE is_deleted = 0
    </select>


</mapper>